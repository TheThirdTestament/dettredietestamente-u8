@inherits Umbraco.Web.Mvc.UmbracoViewPage<Home>
@using ContentModels = Umbraco.Web.PublishedModels;
@{
    Layout = "Master.cshtml";
}

<div class="row">
    <div class="col-12">
        <h1> </h1>
        <div>&nbsp;</div>
    </div>
</div>

<div class="row">
    <div class="col-8">
        @Html.GetGridHtml(Model, "mainContent", "bootstrap3")
    </div>
    <div class="col-4">

        @*

        *@

        @Umbraco.RenderMacro("listNews")

        @Umbraco.RenderMacro("listImageWidgets")

        @*Order form uncomment because email does not work


        @if (Model.DisplayDnvOrderWidget == true)
        {
            @Umbraco.RenderMacro("dnvOrderWidget");
        }
        *@

        @Umbraco.RenderMacro("listEvents")
    </div>
   
</div>
